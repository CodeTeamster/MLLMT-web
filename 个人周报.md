# 个人工作周报
Personal Work Review

项目组：人民当家做组  
姓名：吴晓凡

## 第一周
本周综述：
本周以项目启动和前端需求梳理为主。我参与了启动会需求对齐，重点从前端视角拆解了“多模态推理控制台”的页面信息架构，明确了输入区、结果区、性能监控区和历史查询区的交互关系。结合业界大模型工具的界面形态，初步整理了页面状态清单（空态、加载态、流式输出态、异常态），为后续界面实现提供了基础规范。

本周建议和意见：
考虑到推理任务耗时和并发不确定性，建议在前端提前定义“排队中/推理中/已完成/失败”的统一状态文案与视觉样式，避免后续联调阶段重复返工。

下周计划：
完成开题材料中前端部分内容，并搭建基础前端开发框架，确定组件规范和目录组织方式。

## 第二周
本周综述：
本周完成了前端技术方案确认与基础工程初始化，明确采用 Vue3 + Vite + Element Plus + Pinia 的技术栈。结合现有脚手架结构，我梳理了通用页面模板、路由组织策略和状态管理边界，完成了基础环境联调验证，确保页面开发、接口接入和权限控制可并行推进。

本周建议和意见：
建议前后端尽快统一接口返回体格式（code、data、msg）与错误码语义，减少后续页面异常处理中的分支复杂度。

下周计划：
推进核心页面低保真到高保真的映射，先完成推理主界面和性能看板的页面骨架开发。

## 第三周
本周综述：
本周进入页面结构搭建阶段，完成了推理主界面、性能指标区域和历史记录入口的基础布局。围绕复杂页面的可维护性，我将界面拆分为输入配置、运行状态、结果展示、图表容器等子模块，减少单文件复杂度。同步建立了常用样式变量与基础工具函数，提升后续页面迭代效率。

本周建议和意见：
建议提前冻结第一版页面字段命名和接口参数映射表，避免联调时因字段口径变化导致前端重复改动。

下周计划：
完成登录后主流程页面串联，打通基础导航、权限控制和页面级状态切换。

## 第四周
本周综述：
本周围绕脚手架二次开发完成了前端基础能力落地，包括登录鉴权态处理、菜单路由联动、页面访问权限控制和全局请求拦截。针对通用交互，我补充了统一消息提示和异常反馈处理，保证用户在请求失败、权限不足等场景下有明确反馈。

本周建议和意见：
建议尽早建立前端接口 mock 或稳定测试数据集，以便在后端接口变动期间保障页面开发进度。

下周计划：
进入模型管理与测试集管理页面开发，优先完成列表、检索、分页和详情展示能力。

## 第五周
本周综述：
本周完成了模型管理与测试集管理相关前端页面开发，包含查询筛选、分页加载、表单录入和详情展示等核心能力。针对多模态数据展示，我对长文本、路径字段和状态字段进行了差异化显示处理，提升了信息可读性与操作效率。并完成了与后端初版接口的数据对接。

本周建议和意见：
多模态数据字段长度差异较大，建议后端在返回结构中补充必要的摘要字段，降低前端列表页渲染压力。

下周计划：
重点推进推理触发页面与结果展示页面联调，准备接入流式输出能力。

## 第六周
本周综述：
本周完成推理任务触发流程的页面联调，实现了参数设置、任务提交、运行中反馈和结果回显的基础闭环。针对长耗时请求，我补充了按钮禁用、防重复提交和请求超时提示，避免用户误操作。同步完善了异常回退逻辑，确保失败场景下页面状态可恢复。

本周建议和意见：
当前同步返回方案在体验上存在等待感，建议尽快切换到流式返回机制，并在前端加入增量渲染策略。

下周计划：
接入 SSE 流式数据通道，完成 Token 级别输出的实时展示。

## 第七周
本周综述：
本周完成了 SSE 流式输出在前端的接入与渲染。通过事件流监听实现了推理文本的增量拼接与逐步显示，用户可以实时观察模型生成过程。为提升稳定性，我补充了连接中断提示、重连策略和页面关闭时的连接释放处理，避免资源泄漏。

本周建议和意见：
建议后端在 SSE 事件中统一事件类型和结束标志，便于前端准确区分“正常完成”和“异常中断”状态。

下周计划：
开发 GPU 状态与性能指标的实时可视化区域，完成图表组件接入。

## 第八周
本周综述：
本周聚焦性能监控可视化，完成了 GPU 利用率、显存占用等实时指标图表接入，并实现与推理流同步刷新。针对高频数据更新场景，我对图表刷新频率和数据窗口长度进行了优化，避免图表抖动和页面卡顿。最终实现了推理输出与硬件状态并行展示。

本周建议和意见：
建议控制前后端采样与推送频率在可视化可读与性能开销之间的平衡区间，避免高频更新反向影响推理体验。

下周计划：
完善延迟、吞吐等聚合指标展示，并统一前端数值格式化策略。

## 第九周
本周综述：
本周完成了 TTFT、平均延迟、吞吐量等关键指标在前端的展示与格式化，补齐了指标卡片、趋势图和任务结果摘要区。围绕数据一致性，我规范了毫秒/秒单位展示和保留位数策略，减少用户阅读歧义。同步优化了刷新时机，确保任务结束后指标可稳定落盘并准确回显。

本周建议和意见：
建议后端对所有耗时与吞吐字段提供统一单位说明，前端可据此减少兼容分支，提升可维护性。

下周计划：
开发历史记录与排行榜相关页面，打通从单次推理到结果对比分析的完整路径。

## 第十周
本周综述：
本周完成了推理历史记录页面与多维排行榜页面开发，支持按模型、测试集和性能维度进行筛选与排序。为提升使用体验，我优化了表格信息密度和关键指标高亮规则，使用户能够更快识别高价值结果。同时，我开始对全站配色进行系统化调整，统一主色和强调色表达。

本周建议和意见：
排行榜属于高频访问页面，建议后续评估热点数据缓存方案，避免数据量增长后页面加载变慢。

下周计划：
继续推进全站配色优化和交互细节统一，并配合并发场景联调完善排队状态展示。

## 第十一周
本周综述：
本周重点完成并发场景下的前端状态体验完善。针对“排队中、执行中、已完成、失败”等任务状态，我补充了统一视觉标签、提示文案和操作限制逻辑。配合后端队列机制联调后，页面在高并发触发时能够准确反馈当前位置和任务进度，整体可解释性明显提升。

本周建议和意见：
建议补充任务队列相关的用户引导信息，例如预计等待时长区间或前置任务数量，进一步降低用户不确定感。

下周计划：
进入全链路测试与缺陷修复阶段，重点关注流式渲染稳定性和异常场景恢复能力。

## 第十二周
本周综述：
本周开展了端到端联调测试与缺陷治理，重点修复了流式输出偶发截断、页面状态未及时重置、复杂交互下提示不一致等问题。针对线上可用性，我完善了异常兜底文案和关键操作防抖处理。同步完成一轮配色与细节样式收敛，提升页面一致性与整体观感。

本周建议和意见：
建议建立核心页面回归检查清单，覆盖推理主流程、SSE 中断恢复、历史查询和排行榜筛选，降低版本发布风险。

下周计划：
完成前端文档整理与代码清理，准备项目收尾和答辩演示素材。

## 第十三周
本周综述：
本周以项目收尾为主，完成前端代码清理、注释补充和目录梳理，整理了核心页面的使用说明与联调要点。结合最终演示场景，我对关键页面展示顺序、默认筛选条件和视觉层级进行了微调，保证演示过程连贯稳定。并协助团队完成答辩材料中的前端部分内容。

本周建议和意见：
建议在部署文档中补充前端构建参数、环境变量说明和常见问题排查步骤，降低他人复现成本。

下周计划：
完成项目最终演示与交付复盘，沉淀前端可复用经验与优化建议。

## 第十四周
本周综述：
本周顺利完成项目答辩与演示。演示过程中，多模态推理界面、流式输出、性能看板和历史对比功能运行稳定，页面反馈及时，视觉风格统一。作为前端开发成员，我系统复盘了本次项目中在复杂页面拆分、实时数据渲染、配色体系优化和缺陷闭环处理方面的实践经验，为后续同类项目提供了可参考的实现路径。

本周建议和意见：
后续若进入更高并发和更复杂场景，建议继续推进前端性能优化与可观测性建设，包括长列表虚拟化、图表渲染优化和关键链路埋点分析。

下周计划：
归档项目资产与前端文档，完成个人总结沉淀，正式结束本次实训周期。